
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: nginx
  namespace: default
spec:
  releaseName: nginx
  interval: 5m0s  # This is the field that was missing
  chart:
    spec:
      chart: nginx
      sourceRef:
        kind: HelmRepository
        name: stable-charts
        namespace: flux-system
  values:
    service:
      type: LoadBalancer

